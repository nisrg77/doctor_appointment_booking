{
  "name": "mern",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npm install --prefix appointment-backend && npm install --prefix appointment-frontend && npm run build --prefix appointment-frontend",
    "start": "node appointment-backend/index.js",
    "dev:server": "npm start --prefix appointment-backend",
    "dev:client": "npm run dev --prefix appointment-frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "mongoose": "^9.4.1"
  }
}
